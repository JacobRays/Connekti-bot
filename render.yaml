services:
  - type: web
    name: connekti-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PAYPAL_EMAIL
        sync: false
      - key: BASE_URL
        sync: false
